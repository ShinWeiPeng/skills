interface:
  display_name: "Modular Event Architecture"
  short_description: "Govern flow evidence, ownership, and real-time design."
  default_prompt: "Use $govern-modular-event-architecture to review Flow cost and model evidence, then pass pre-code boundary, type, state, execution-efficiency, and workload-driven real-time scheduling gates before implementation."

policy:
  allow_implicit_invocation: true
