# Governed Engineering Skills 0.5.0-beta.1 validation evidence

- Date: 2026-08-02
- Change set: `canonical-spec-governance`
- Fixed point: `main@1d8385b` plus the authorized
  `feat/canonical-spec-governance-0.5` change set
- ADR status: `ADR-0011` remains proposed pending human approval

## Unit and contract validation

- Command: `python -m unittest discover -s tests -p "test_*.py" -v`
- Exit code: `0`
- Minimal raw output: `Ran 112 tests in 4.554s ... OK`
- Verdict: `PASS`

Coverage includes authorization-before-write, stable REQ/DEC/AC IDs across updates,
removed-ID deltas, cross-round relationship preservation, structural validation,
requirement coverage, invalid relations, open decisions, tracker failure preservation,
active-spec precedence, multiple-candidate blocking, branch resolution, cross-task
verification, scope-creep blocking, and review-to-implemented evidence.

## Branch integrity

- `git merge-base --is-ancestor main HEAD`
  - Exit code: `0`
  - Output: `PASS: main is an ancestor of HEAD`
- `git diff --check main...HEAD`
  - Exit code: `0`
  - Output: no diagnostics
- `git diff --name-status main...HEAD`
  - Exit code: `0`
- Observation: feature paths are under `plugins/governed-engineering-skills`;
    the root `.changeset/canonical-spec-governance.md` records the repository-level
    release plan. Unrelated `CLAUDE.md` is absent.
- Verdict: `PASS`

## Integration validation

- Command: `python scripts/validate_integration.py`
- Exit code: `0`
- Minimal raw output:
  `PASS: 28 skills; version metadata consistent; HackMD isolated; no user-absolute paths`
- Verdict: `PASS`
- Python validation used `PYTHONDONTWRITEBYTECODE=1`; no cache deletion was required.

## Installer validation

- Command:
  `powershell.exe -NoLogo -NoProfile -ExecutionPolicy Bypass -File tests/test_install_local.ps1`
- Exit code: `0`
- Minimal raw output:
  `PASS: marketplace registration and Codex Desktop launch scenarios`
- Verdict: `PASS`

## Architecture validation

- Development command:
  `python tools/architecture/architecture_cli.py gate --phase development --manifest architecture/manifest.yaml --adoption architecture/adoption.yaml --baseline architecture/baseline.yaml --format text`
- Development gate exit code: `0`
- Development output: `PASS: VERIFIED (phase=development, exit=0)`
- Release command:
  `python tools/architecture/architecture_cli.py gate --phase release --manifest architecture/manifest.yaml --adoption architecture/adoption.yaml --baseline architecture/baseline.yaml --format text`
- Release gate exit code: `0`
- Release output: `PASS: VERIFIED (phase=release, exit=0)`
- Generated views: rendered from `architecture/manifest.yaml`; the subsequent
  development gate reported no stale generated document.
- Deterministic render check:
  `PASS: deterministic render produced identical generated-view hashes (9 files)`.
- Verdict: `PASS`

The remaining `SYM004` diagnostics are unchanged SHOULD-level notices for Markdown
symbols without installed language analyzers. No baseline debt was added.

## Vendor and version validation

- `python scripts/vendor_sync.py --check`
  - Exit code: `0`
  - Output: `PASS: 28 pinned skill snapshots match vendor-lock.json`
- `python scripts/version_governance.py check`
  - Exit code: `0`
  - Output: `PASS: governed plugin version metadata is consistent`
- Current checked-in release metadata remains `0.4.0-beta.1`. The pending plugin
  changeset and `release-intent.json` target `0.5.0-beta.1` as a new minor
  prerelease group with high risk; the shared Version workflow applies that intent.
- Pinned upstream source hashes remain unchanged; only intentional integrated overlay
  hashes and the new integration-owned skill entry changed.
- Verdict: `PASS`

## Diff hygiene and review

- `git diff --check`
  - Exit code: `0`
  - Output: no diagnostics
- Review fixed point: `git diff main...HEAD` and
  `git log main..HEAD --oneline`.
- Standards axis: `PASS`; no documented-standard violation or actionable smell.
- Spec axis: `PASS`; no missing or partial requirement, incorrect behavior, or
  scope creep.
- Governance prerequisites: development and release architecture gates are `PASS`;
  runtime evidence is not applicable to this change set.

The feature change has not been tagged, installed, published, or deployed.
Marketplace registration and old-worktree cleanup remain explicitly deferred.
